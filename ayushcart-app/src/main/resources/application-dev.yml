spring:
  datasource:
    url: jdbc:h2:mem:ayushcart;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop   # schema is rebuilt on every start; data is re-seeded
    show-sql: false
